# LoLSmartStatsWeb - Frontend

这是 LoLSmartStatsWeb 的前端项目，基于 Vue 3、TypeScript 和 Vite 构建。

## 🛠️ 技术栈

*   **框架**: Vue 3.5+
*   **语言**: TypeScript 5.9+
*   **构建工具**: Vite 7.2+
*   **包管理器**: npm (或 yarn/pnpm)

## 📦 安装依赖

首次运行前，请确保已安装 Node.js，并在 `frontend` 目录下安装项目依赖：

```bash
npm install
```

## 🚀 开发指南

### 启动开发服务器

```bash
npm run dev
```

命令执行后，Vite 会启动一个本地开发服务器（默认端口通常为 `5173`），支持热重载（HMR）。

### 构建生产版本

```bash
npm run build
```

该命令会进行类型检查（`vue-tsc`）并打包应用到 `dist` 目录，用于生产环境部署。

### 本地预览生产构建

```bash
npm run preview
```

在构建完成后，可以使用此命令在本地预览打包后的应用。

## 📁 目录结构

```
frontend/
├── public/             # 静态资源目录 (不经过 Vite 处理)
├── src/
│   ├── assets/         # 静态资源 (图片、字体等)
│   ├── components/     # Vue 组件
│   ├── App.vue         # 根组件
│   ├── main.ts         # 入口文件
│   └── style.css       # 全局样式
├── index.html          # HTML 模板
├── package.json        # 项目依赖与脚本
├── tsconfig.json       # TypeScript 配置
└── vite.config.ts      # Vite 配置
```

## ⚙️ 配置说明

*   **Vite 配置**: 修改 `vite.config.ts` 可以调整构建选项、代理设置等。
*   **TypeScript 配置**: 修改 `tsconfig.json` 可以调整 TS 编译选项。
