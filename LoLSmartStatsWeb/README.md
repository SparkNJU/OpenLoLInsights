# LoLSmartStatsWeb

LoLSmartStatsWeb 是 OpenLoLInsights 项目的 Web 展示端，采用前后端分离架构。前端使用 Vue 3 + TypeScript + Vite 构建，后端使用 Java Spring Boot 框架。

## 📁 项目结构

```
LoLSmartStatsWeb/
├── backend/            # 后端项目 (Spring Boot)
│   ├── src/            # Java 源代码
│   ├── pom.xml         # Maven 依赖配置
│   └── ...
├── frontend/           # 前端项目 (Vue 3 + Vite)
│   ├── src/            # Vue 组件与 TypeScript 代码
│   ├── package.json    # npm 依赖配置
│   ├── vite.config.ts  # Vite 配置
│   └── ...
└── README.md           # 项目总说明文档
```

## 🛠️ 环境要求

在运行本项目之前，请确保您的开发环境满足以下要求：

*   **Java**: JDK 17 或更高版本
*   **Maven**: 3.6 或更高版本
*   **Node.js**: 18.0 或更高版本 (推荐 LTS)
*   **npm**: 通常随 Node.js 安装

## 🚀 快速启动

### 1. 启动后端服务

后端服务默认运行在 `8080` 端口。

```bash
cd backend
# 使用 Maven 运行
mvn spring-boot:run
```

### 2. 启动前端开发服务器

前端开发服务器默认运行在 `5173` 端口（Vite 默认）。

```bash
cd frontend
# 安装依赖
npm install
# 启动开发服务器
npm run dev
```

启动成功后，访问终端输出的本地地址（通常是 `http://localhost:5173`）即可查看应用。

## 📚 详细文档

*   [后端详细文档](./backend/README.md)
*   [前端详细文档](./frontend/README.md)
